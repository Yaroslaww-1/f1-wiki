import React from 'react';
import { Spinner as SpinnerBootstrap } from 'react-bootstrap';

const Spinner: React.FC = () => {
  return <SpinnerBootstrap animation="border" />;
};

export default Spinner;
